import { Link } from 'react-router-dom'
import Callout from '../components/Callout.jsx'

export default function Home() {
  return (
    <div className="page">
      <div className="page-head">
        <span className="badge">Обучающий стенд по безопасности веб-приложений</span>
        <h1>Межсайтовый скриптинг (XSS)</h1>
        <p className="lead">
          XSS (Cross-Site Scripting) — внедрение злоумышленником стороннего
          JavaScript-кода в страницу, которую видят другие пользователи.
          Выполненный в браузере жертвы код имеет доступ к её cookie, сессии,
          содержимому страницы и действиям от её имени.
        </p>
      </div>

      <Callout type="info" title="Как пользоваться стендом">
        <p>
          Для каждого подтипа XSS показаны 5 этапов: <b>где уязвимость и
          уязвимый код → практическая атака на тренировочных данных → как это
          видит жертва → исправление кода → безопасный вид</b>. На каждой
          странице есть переключатель <b>«Уязвимый ⇄ Безопасный режим»</b> —
          он меняет и показанный код, и реальное поведение демонстрации.
        </p>
      </Callout>

      <div className="cards">
        <Link to="/stored" className="card">
          <div className="card-num">Подтип 1</div>
          <h3>Хранимый (постоянный) XSS</h3>
          <p>
            Payload сохраняется на сервере (в БД) и срабатывает у каждого, кто
            открывает страницу. Пример: комментарии / гостевая книга.
          </p>
        </Link>
        <Link to="/reflected" className="card">
          <div className="card-num">Подтип 2</div>
          <h3>Отражённый (непостоянный) XSS</h3>
          <p>
            Payload передаётся в URL и «отражается» сервером в ответ. Срабатывает
            у того, кто перешёл по вредоносной ссылке. Пример: поиск.
          </p>
        </Link>
        <Link to="/dom" className="card">
          <div className="card-num">Подтип 3</div>
          <h3>DOM-модель (XSS на клиенте)</h3>
          <p>
            Payload обрабатывается целиком в браузере (например, из части URL
            после&nbsp;<span className="mono">#</span>). Сервер его даже не видит.
          </p>
        </Link>
      </div>

      <Callout type="warning" title="Этика и правовые рамки">
        <p>
          Стенд предназначен <b>только для обучения</b> в изолированной среде на
          тренировочных данных. Применять подобные приёмы к чужим сайтам без
          письменного разрешения владельца — противоправно.
        </p>
      </Callout>

      <Callout type="success" title="Главный принцип защиты от XSS">
        <p>
          <b>Никогда не доверяйте пользовательскому вводу и кодируйте/очищайте
          данные в точке вывода.</b> Текст нужно выводить как текст, а не как
          HTML. В React это значит избегать{' '}
          <span className="mono">dangerouslySetInnerHTML</span>; на сервере —
          экранировать вывод и/или применять санитайзер.
        </p>
      </Callout>
    </div>
  )
}
